@extends('layouts.app')
@section('title','Usuários')
@section('subtitle','Acessos individuais para a rotina do consultório.')
@section('content')<div class="d-flex justify-content-end mb-3"><a class="btn btn-primary" href="{{ route('users.create') }}">Novo usuário</a></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Nome</th><th>E-mail</th><th>Perfil</th><th>Situação</th><th><span class="visually-hidden">Ações</span></th></tr></thead><tbody>@foreach($users as $account)<tr><td class="fw-semibold">{{ $account->name }}</td><td>{{ $account->email }}</td><td>{{ $account->role->name }}</td><td><span class="badge {{ $account->active?'text-bg-success':'text-bg-secondary' }}">{{ $account->active?'Ativo':'Inativo' }}</span></td><td>@if(!$account->isOwner())<a class="btn btn-sm btn-outline-secondary" href="{{ route('users.edit',$account) }}" aria-label="Editar {{ $account->name }}">Editar</a>@else<span class="text-secondary small">Proprietário</span>@endif</td></tr>@endforeach</tbody></table></div><div class="card-footer">{{ $users->links() }}</div></div>@endsection
