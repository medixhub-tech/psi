@extends('layouts.app')
@section('title','Perfis de acesso')
@section('subtitle','Permissões claras para cada responsabilidade.')
@section('content')<div class="d-flex justify-content-end mb-3"><a class="btn btn-primary" href="{{ route('roles.create') }}">Novo perfil</a></div><div class="row g-4">@foreach($roles as $role)<div class="col-lg-6"><div class="card h-100"><div class="card-body p-4"><h2 class="h5">{{ $role->name }}</h2><p class="text-secondary small">{{ $role->users_count }} usuário(s) vinculado(s)</p>@if($role->code==='psychologist')<p>Acesso exclusivo do profissional responsável, incluindo prontuário e gestão financeira.</p><span class="badge text-bg-light">Perfil protegido</span>@else<ul class="permission-list">@forelse($role->permissions as $permission)<li>{{ $permission->name }}</li>@empty<li>Sem permissões operacionais.</li>@endforelse</ul><a class="btn btn-outline-secondary btn-sm" href="{{ route('roles.edit',$role) }}">Editar permissões</a>@endif</div></div></div>@endforeach</div>@endsection
